[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    slow: tests that use the large hg_test dataset (294 MB+ .bumbl)
    requires_refs: tests that need reference FASTA files from the .lengths manifest
addopts = -ra --strict-markers
